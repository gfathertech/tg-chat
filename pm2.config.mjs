export default {
  apps: [
    {
      name: "amatullah",
      script: "bun",               // Idk if I should leave this
      args: ["bun index.ts"],      // added this in args
      watch: false
    }
  ]
}
